package org.bdgenomics.guacamole

/**
 * Step through loci in the genome and at each step consider all the reads (or other read-like objects) that overlap the
 * locus.
 */
package object windowing {}
