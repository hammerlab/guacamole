package org.hammerlab.guacamole

/**
 * Implementations of likelihood models.
 *
 * A likelihood model answers the question: if a certain genotype is true, what is the probability of observing the
 * given reads?
 */
package object likelihood {}
